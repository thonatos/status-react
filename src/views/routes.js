import { routes as docker } from './docker/'
import { routes as clusters } from './clusters/'
import { routes as images } from './images/'
import { routes as trends } from './trends/'

export { docker, clusters, images, trends }
